import React from 'react'               // Imports the react library

// Creates a function for the resources page for us to use later in the app.jsx file (Old figure out if needed)
function Resources() {
  return (
    <div>Resources</div>                // Displays the word "Resources" on the page
  );
}

export default Resources;               // Exports the function for later use 