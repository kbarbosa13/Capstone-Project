import React from 'react';                                               // Imports the React library 
import RedirectButton from "../RedirectButtons/RedirectButton.jsx";      // Imports the RedirectButton.jsx file as the variable RedirectButton 
import "../RedirectButtons/RedirectButton.css";                          // Imports the RedirectButton.css file


function Home() {
   return (
    <div>Home</div>                // Displays the word "Home" on the page
  );
}

export default Home;             // Export the Home function for later use in app.jsx
