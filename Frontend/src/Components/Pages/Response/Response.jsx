import React from 'react'                // Imports the react library 

// Creates the Response function used to display information on the response page (Old figure out if needed)
function Response() {
  return (
    <div>Response</div>                  // Displays the word "Response" on the page
  );
}

export default Response;                 // Exports the function for later use in app.jsx
